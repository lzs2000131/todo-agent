# Todo Agent - Mac 待办应用设计方案

## 技术栈确定

| 类别 | 选型 | 说明 |
|------|------|------|
| 框架 | **Tauri 2.0 + React 18** | 体积小、性能好 |
| 语言 | TypeScript + Rust | 前后端类型安全 |
| UI | Tailwind CSS + Framer Motion | 清新动画 |
| 状态 | Zustand | 轻量状态管理 |
| 存储 | SQLite | 本地持久化 |
| 同步 | AWS S3 | 云端备份同步 |
| AI | OpenAI GPT-4o-mini | 用户自配置 API Key |

---

## 功能模块

### 1. 核心待办功能
- 添加/编辑/删除待办
- 标记完成/未完成
- 优先级设置（高/中/低）
- 截止日期设置

### 2. 分类与标签
- 自定义分类（工作、生活、学习等）
- 多标签支持
- 按分类/标签筛选

### 3. 提醒通知
- 到期提醒（系统通知）
- 自定义提醒时间
- 重复提醒设置

### 4. 截图识别
- 全局快捷键：`Cmd + Shift + T`
- 区域截图选择
- OpenAI Vision 自动提取待办
- 预览确认后或者自动添加待办

### 5. 云端同步 (S3)
- 自动备份到 S3
- 多设备同步
- 冲突处理机制

### 6. 设置页面
- OpenAI API Key 配置
- AWS S3 凭证配置
- 快捷键自定义
- 主题切换

---

## 数据模型

```typescript
// 待办项
interface Todo {
  id: string;
  title: string;
  description?: string;
  completed: boolean;
  priority: 'high' | 'medium' | 'low';
  categoryId?: string;
  tags: string[];
  dueDate?: Date;
  reminder?: Date;
  createdAt: Date;
  updatedAt: Date;
}

// 分类
interface Category {
  id: string;
  name: string;
  color: string;
  icon?: string;
}

// 用户设置
interface Settings {
  openaiApiKey?: string;
  s3Config?: {
    bucket: string;
    region: string;
    accessKeyId: string;
    secretAccessKey: string;
  };
  screenshotShortcut: string;
  theme: 'light' | 'dark' | 'system';
  syncEnabled: boolean;
}
```

---

## 项目结构

```
todo-agent/
├── src/                        # React 前端
│   ├── components/
│   │   ├── todo/
│   │   │   ├── TodoList.tsx
│   │   │   ├── TodoItem.tsx
│   │   │   ├── TodoForm.tsx
│   │   │   └── TodoFilters.tsx
│   │   ├── category/
│   │   │   ├── CategoryList.tsx
│   │   │   └── CategoryForm.tsx
│   │   ├── screenshot/
│   │   │   ├── ScreenshotOverlay.tsx
│   │   │   └── ExtractPreview.tsx
│   │   ├── settings/
│   │   │   ├── SettingsPage.tsx
│   │   │   ├── ApiKeyConfig.tsx
│   │   │   └── S3Config.tsx
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Modal.tsx
│   │       └── Toast.tsx
│   ├── hooks/
│   │   ├── useTodos.ts
│   │   ├── useCategories.ts
│   │   ├── useSettings.ts
│   │   └── useScreenshot.ts
│   ├── services/
│   │   ├── openai.ts           # OpenAI API 调用
│   │   ├── s3.ts               # S3 同步服务
│   │   └── notification.ts     # 系统通知
│   ├── stores/
│   │   ├── todoStore.ts
│   │   ├── categoryStore.ts
│   │   └── settingsStore.ts
│   ├── lib/
│   │   ├── db.ts               # SQLite 操作
│   │   └── utils.ts
│   ├── styles/
│   │   └── globals.css
│   ├── App.tsx
│   └── main.tsx
│
├── src-tauri/                  # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── commands/
│   │   │   ├── mod.rs
│   │   │   ├── screenshot.rs   # 截图命令
│   │   │   ├── db.rs           # 数据库命令
│   │   │   └── notification.rs # 通知命令
│   │   └── utils/
│   │       └── mod.rs
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   └── capabilities/
│       └── default.json
│
├── package.json
├── tsconfig.json
├── tailwind.config.js
├── vite.config.ts
└── README.md
```

---

## 界面设计

### 配色方案 (清新活泼)

```css
:root {
  --primary: #6366F1;      /* 靛蓝 - 主色调 */
  --primary-light: #818CF8;
  --success: #10B981;      /* 绿色 - 完成状态 */
  --warning: #F59E0B;      /* 橙黄 - 高优先级 */
  --danger: #EF4444;       /* 红色 - 紧急/删除 */
  --bg-primary: #F8FAFC;   /* 主背景 */
  --bg-card: #FFFFFF;      /* 卡片背景 */
  --text-primary: #1E293B;
  --text-secondary: #64748B;
}
```

### 布局草图

```
┌─────────────────────────────────────────────────┐
│  Todo Agent                    [设置] [截图]    │
├─────────────┬───────────────────────────────────┤
│             │                                   │
│  全部 (12)  │  ┌─────────────────────────────┐  │
│  今天 (3)   │  │ + 添加新待办...             │  │
│  即将到期(5)│  └─────────────────────────────┘  │
│             │                                   │
│  ─────────  │  ┌─────────────────────────────┐  │
│  分类       │  │ ○ 完成项目方案设计    🔴    │  │
│  📁 工作    │  │   截止: 今天 | #工作        │  │
│  📁 生活    │  └─────────────────────────────┘  │
│  📁 学习    │                                   │
│             │  ┌─────────────────────────────┐  │
│  ─────────  │  │ ○ 回复客户邮件        🟡    │  │
│  标签       │  │   截止: 明天 | #工作        │  │
│  #紧急      │  └─────────────────────────────┘  │
│  #会议      │                                   │
│             │  ┌─────────────────────────────┐  │
│             │  │ ✓ 买牛奶              🟢    │  │
│             │  │   已完成 | #生活            │  │
│             │  └─────────────────────────────┘  │
│             │                                   │
└─────────────┴───────────────────────────────────┘
```

---

## 实现阶段

### Phase 1: 项目搭建
- 初始化 Tauri + React + TypeScript 项目
- 配置 Tailwind CSS、Vite
- 基础 UI 组件库

### Phase 2: 待办核心功能
- SQLite 数据库集成
- 待办 CRUD 功能
- 分类和标签系统
- 列表/筛选界面

### Phase 3: 界面美化
- 完善 UI 组件样式
- Framer Motion 动画
- 响应式布局

### Phase 4: 截图 + AI 识别
- 全局快捷键注册
- 区域截图实现
- OpenAI Vision API 集成
- 提取结果确认流程

### Phase 5: 提醒通知
- 系统通知集成
- 定时提醒调度
- 通知权限处理

### Phase 6: S3 云端同步
- S3 SDK 集成
- 自动备份机制
- 多设备同步逻辑
- 冲突处理

### Phase 7: 打包发布
- 应用图标设计
- DMG 打包配置
- 自动更新支持

---

## 依赖清单

### 前端 (package.json)
```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-sql": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0",
    "zustand": "^5.0.0",
    "framer-motion": "^11.0.0",
    "lucide-react": "^0.460.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "openai": "^4.70.0",
    "date-fns": "^4.1.0",
    "clsx": "^2.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

### 后端 (Cargo.toml)
```toml
[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-notification = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

---

## 下一步

确认此方案后，我将开始：
1. 初始化 Tauri 2.0 项目
2. 配置开发环境
3. 逐步实现各功能模块

是否开始实施？
